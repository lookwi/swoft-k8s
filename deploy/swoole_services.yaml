apiVersion: v1
kind: Service
metadata:
  name: swoole-svc
  labels:
    app: swoole-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: swoole-app
